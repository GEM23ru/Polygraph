# 🔄 HANDOFF — Polygraph (передача дел)

> Этот документ нужен, чтобы любой новый агент или разработчик за 5 минут
> понял проект, что уже сделано, где грабли и что делать дальше.
> Если в текущем чате что-то пошло не так — начни отсюда.

**Дата последнего обновления:** 2026-06-03
**Владелец:** GEM23ru · **Репозиторий:** https://github.com/GEM23ru/Polygraph
**Локальный путь у пользователя:** `C:\Полиграф\polygraph` (Windows, PowerShell)

---

## 1. ЧТО ЭТО ЗА ПРОЕКТ

**Polygraph** — мульти-провайдерный AI-агент на Python. Цель пользователя:
сделать агента, который умеет то же, что ассистент Arena (поиск, чтение
веб-страниц, выполнение кода, работа с файлами, рассуждения), а в перспективе —
генерация изображений/видео, парсинг Telegram, инфографика для маркетплейсов.

Идея архитектуры: одно ядро (`core.py`) общается с 6 провайдерами LLM и
само маршрутизирует запрос на подходящую модель. Сверху — агентский слой
(`agent.py`) с инструментами (function calling).

---

## 2. СТРУКТУРА ФАЙЛОВ

| Файл | Назначение |
|------|-----------|
| `core.py` | Ядро: провайдеры, 20 моделей, роутинг, fallback, кэш, ансамбль |
| `agent.py` | Агент: function calling + 8 инструментов |
| `web.py` | 🌐 Веб-чат в браузере (Flask, http://127.0.0.1:5000) — главный UI |
| `Запустить Polygraph.bat` | Запуск с чёрным окном (видно сервер) |
| `Polygraph (без окна).vbs` | Запуск БЕЗ окна (фоном) — для ярлыка на раб. столе |
| `polygraph.ico` / `polygraph_icon.png` | Иконка для ярлыка на рабочем столе |
| `КАК_ЗАПУСКАТЬ.txt` | Инструкция по запуску и созданию ярлыка с иконкой |
| `chat.py` | 💬 Интерактивный чат в консоли |
| `healthcheck.py` | 🩺 Проверка, какие модели реально живы с текущими ключами |
| `run.py` | Демо всех возможностей ядра |
| `.env` | Ключи API (НЕ в git!). Создаётся из `.env.example` |
| `.env.example` | Шаблон ключей со ссылками |
| `requirements.txt` | Зависимости: openai, python-dotenv, google-genai, groq, cohere |
| `README.md` | Инструкция для пользователя |
| `HANDOFF.md` | Этот файл |

**Запуск:** `python web.py` (браузер) или `python chat.py` (консоль).
Проверка: `python healthcheck.py --free`.

**web.py** — Flask-сервер, HTML встроен в файл (PAGE), без внешних зависимостей
(работает офлайн). Боковая панель с историей чатов (сворачивается кнопкой ☰).
Несколько чатов хранятся в CHATS и сохраняются в chats.json (в .gitignore).
Эндпоинты: `/`, `/api/chats` (список), `/api/chat/<id>` (получить), `/api/new`,
`/api/delete/<id>`, `/api/chat` (POST: message+model+chat_id). Заголовок чата =
начало первого вопроса. Выбор модели через дропдаун → agent.force_model.
Запуск: .bat (с окном) или .vbs (без окна, для ярлыка). web.py сам открывает
браузер, когда порт готов (socket-проверка). Клиенты провайдеров ЛЕНИВЫЕ
(создаются при первом вызове) — старт быстрый. Остановка .vbs: убить pythonw.exe.
- **Режим разработки (2026-06-05):** `python web.py --dev` (или PG_DEV=1, или
  «Polygraph (разработка).bat») — Flask debug + use_reloader: сервер сам
  подхватывает правки в .py, перезапуск не нужен (F5 в браузере). Браузер
  открывается 1 раз (через WERKZEUG_RUN_MAIN). Для обычного юзера — .vbs (без reload).
  «Остановить Polygraph.bat» убивает python/pythonw без Диспетчера задач.
- **Загрузка файлов (2026-06-04):** в чате кнопка ➕ + drag&drop по всему окну.
  Эндпоинт /api/upload сохраняет в agent_files/uploads/, /agent_files/<path>
  отдаёт картинки для превью. Картинки → analyze_image, документы → read_file
  (read_file умеет PDF через pypdf). Файлы шлются в /api/chat как files[].
Фикс 2026-06-04: промпт здоровается ТОЛЬКО в первом сообщении беседы.
ВАЖНО про .bat: только латиница в командах (русский текст ломает Windows как
команды), формат CRLF, без BOM. Запуск по иконке протестирован — РАБОТАЕТ.
Иконка polygraph.ico (P из схемы на шестиугольнике), исходник polygraph_icon.png.
- **Редизайн UI (2026-06-04):** приветственный экран в стиле Claude/ChatGPT —
  центрированный логотип + «Чем могу помочь?» + 4 карточки-подсказки (клик =
  отправка). Поле ввода с иконкой-самолётиком. ДВЕ ТЕМЫ (тёмная/светлая) через
  data-theme + кнопка 🌙/☀️, выбор сохраняется в localStorage. Голубой акцент
  (#22d3ee) в тон иконке. Favicon во вкладке: эндпоинт /favicon.ico отдаёт
  polygraph.ico. Анимации: плавное появление сообщений, парящий логотип.
  Превью дизайна можно собрать в preview_ui.html (data-URI иконки).

---

## 3. ПРОВАЙДЕРЫ И КЛЮЧИ (КРИТИЧНО!)

Пользователь находится в **РОССИИ**. Это определяет всё:

| Провайдер | Из РФ без VPN | Free-лимит | Статус у пользователя |
|-----------|:---:|-----------|----------------------|
| OpenRouter | ✅ | ~50 запросов/день | ключ есть, лимит часто исчерпан |
| Google (Gemini) | ✅ | ~250/день | ключ есть, лимит часто исчерпан |
| **Groq** | ❌ **только VPN** | 14 400/день, очень быстрый | ключ есть, работает с VPN |
| Mistral | ✅ | free tier | ключа пока нет |
| Cohere | ✅ | ~1000/мес | ключа пока нет |
| Cerebras | ⚠️ часто блокирует РФ | — | ключа нет |

**ГЛАВНЫЙ ВЫВОД:** сейчас рабочая лошадка — **Groq + включённый VPN**.
Без VPN Groq отдаёт `Forbidden`. OpenRouter/Google работают без VPN,
но их дневной лимит легко исчерпывается тестами (429 = лимит, 503 = провайдер занят).

Бюджет пользователя: **только бесплатное** (платные API не используем).

---

## 4. ✅ ЧТО УЖЕ СДЕЛАНО

### Исправленные баги (история)
1. **Gemini вызывал инструмент дважды** — исправлено в `_run_via_google`.
2. **Calculator падал с TypeError** — параметр схемы `expression` не совпадал
   с именем аргумента функции. Исправлено + добавлена фильтрация аргументов
   по сигнатуре в `Tool.call` (защита от любых рассогласований).
3. **Мёртвые модели OpenRouter (404)** — все 7 старых ID устарели, заменены
   на живые (проверены через https://openrouter.ai/api/v1/models).
4. **Мёртвая модель Groq** `mixtral-8x7b-32768` (снята) → заменена.
5. **Groq Kimi 404** — ID сменился на `moonshotai/kimi-k2-instruct-0905`.
6. **Молчаливое проглатывание ошибок** — добавлен флаг `debug` с логами.
7. **Google rate-limit ломался при параллельных вызовах** — добавлен `threading.Lock`.
8. **Роутер зависал на 44с** на провайдерах без ключа → теперь мгновенно
   пропускает недоступных (`_is_available`).

### Добавленные возможности
- **web_search улучшен (2026-06-03):** трёхуровневый — Tavily (если ключ) →
  DuckDuckGo Instant Answer → Wikipedia API (надёжный fallback без ключа).
  Раньше DuckDuckGo один часто отдавал «ничего не найдено».
- **deep_research (2026-06-03):** инструмент «глубокого исследования» — делает
  поиск через Tavily + читает 2-3 топ-страницы целиком (fetch_url) + собирает
  материал со ССЫЛКАМИ. Системный промпт велит агенту указывать источники.
  Это приближает поведение к ассистенту-ресёрчеру. Tavily-ключ у пользователя ЕСТЬ.
- **Зрение fallback-цепочка (2026-06-04):** старые OpenRouter vision-модели
  (qwen2.5-vl, llama-3.2-vision) УСТАРЕЛИ. Сейчас: Gemini 2.5-flash → 2.0-flash →
  2.5-pro → OpenRouter (gemma-4-31b/26b, nemotron-nano-vl, kimi-k2.6) — все free.
  Много попыток, чтобы лимит Gemini (10/мин) не ломал зрение. Загрузчик картинок
  с нормальным User-Agent+Accept. Wikipedia /thumb/ блокируется — юзать оригиналы.
- **Зрение / analyze_image (2026-06-04):** агент умеет «видеть» изображения —
  инструмент analyze_image принимает путь к файлу (в agent_files/) или URL картинки
  и анализирует через Gemini vision (gemini-2.5-flash) или OpenRouter vision
  (qwen2.5-vl-72b/32b, llama-3.2-11b-vision — все free). Полезно для разбора
  карточек товаров/инфографики/скриншотов. Работает из РФ (Gemini) без VPN.
- **Генерация изображений (2026-06-05):** инструмент generate_image. Основной
  способ — Gemini native (gemini-2.5-flash-image «nano-banana»), запасной —
  Pollinations (часто платный 402, но из РФ может работать). Картинка сохраняется
  в agent_files/generated/, инструмент возвращает маркер "IMAGE:путь|подпись".
  web.py распознаёт маркер (regex), кладёт URL в поле images[] ответа, фронт
  показывает картинку в чате с кнопкой скачать. Промпт агент переводит на англ.
  ВАЖНО: генерация завязана на Gemini-ключ (Pollinations ненадёжен).
- **11 инструментов** в `agent.py`: `generate_image`, `deep_research`, `analyze_image`, `web_search` (Tavily/DuckDuckGo/Wikipedia),
  `fetch_url` (чтение веб-страниц), `run_python` (выполнение кода),
  `read_file`/`write_file`/`list_files` (файлы в `agent_files/`),
  `calculator`, `datetime_now`.
- **Универсальный движок function calling** `_run_via_openai_compat` —
  работает с Groq/OpenRouter/Cerebras/Mistral. Порядок приоритета:
  **Groq → OpenRouter → Cerebras → Mistral → Gemini**.
- **CLI-чат** `chat.py` с памятью диалога (6 пар) и командами
  `/help /tools /models /model /clear /stats /exit`. Показывает, какая модель ответила.
- **Команда /model** — переключение модели на лету: `/model gpt-oss` (грамотный
  русский, по умолчанию), `/model gemini`, `/model llama`, `/model fast`,
  `/model qwen`, `/model auto`. Алиасы в `Agent.MODEL_ALIASES`.
- **gpt-oss-120b сделана приоритетной** в function calling — грамотнее держит
  русский, чем llama-3.3 (которая вставляла англ. слова типа 'larger').
- Промпт усилен: требует развёрнутые ответы с фактами/цифрами из источников
  и СТРОГО русский язык.
- **Человечный стиль + уточнения (2026-06-03):** промпт переписан — агент общается
  живо, на «ты», подстраивает длину ответа под вопрос (короткий вопрос → короткий
  ответ, большой разбор только когда нужен), предлагает след. шаги и ЗАДАЁТ
  уточняющие вопросы при расплывчатых запросах. Все рабочие функции (поиск,
  источники, защита от галлюцинаций) сохранены. Память диалога в chat.py (6 пар)
  обеспечивает связность уточнений.
- **Защита от двойного запуска (2026-06-06):** баг — фоновый .vbs-сервер (без окна)
  оставался работать, юзер запускал ещё один (.bat/.vbs), порт 5000 занят, браузер
  общался со СТАРЫМ сервером (старый код) → казалось, что фикс не работает. Решено:
  web.py при старте проверяет, занят ли порт 5000; если да — не запускает второй
  сервер, просто открывает браузер на работающий + подсказка «сначала Остановить».
  ВАЖНО для отладки: всегда «Остановить Polygraph.bat» перед новым запуском!
- **Закрепление модели на сессию (2026-06-05):** раньше модели «прыгали» между
  сообщениями (gpt-oss→llama→gemini), что ломало стиль и память. Теперь в авто-режиме
  первая успешно ответившая модель ЗАКРЕПЛЯЕТСЯ (Agent.session_model) и ведёт весь
  диалог (как Arena-ассистент: одна модель на сессию). Сброс: новый чат / смена модели
  в дропдауне / падение модели (тогда берётся следующая и закрепляется она).
  АРХИТЕКТУРА: инструменты (навыки) ОБЩИЕ для всех моделей — добавляешь навык один раз
  в default_tools(), пользуются все. Прокачка агента = добавление инструментов.
- **Фикс «llama бредит про картинки» (2026-06-06):** llama-3.3-70b на текстовых
  запросах галлюцинировала «пришли изображение для analyze_image». Решено: llama
  убрана из приоритета (теперь gpt-oss-120b → gpt-oss-20b → qwen3-32b → llama в конце);
  в промпт добавлен явный запрет упоминать картинки/просить изображение, если в
  запросе картинок нет. Сама подсказка про файлы из истории (refers_to_file) была ОК —
  баг был в слабой модели.
- **Алгоритм поиска как у Arena (2026-06-06):** в промпт добавлен 5-шаговый алгоритм
  качественного поиска (точный запрос с номерами/годом → несколько попыток →
  приоритет офиц. источников → отличать «упоминание» от «ответа» → честный итог).
  deep_research улучшен в КОДЕ: делает несколько запросов (основной + вариация),
  собирает URL из всех, СОРТИРУЕТ по приоритету (офиц. домены .gov/seller.wb/nalog/
  consultant первыми), читает топ-3. Промпт ~10.4К символов (большой — следить).
- **PDF по ссылке + Cloudflare генерация (2026-06-06):** fetch_url теперь извлекает
  текст из PDF (pypdf) при PDF-сигнатуре/.pdf — раньше показывал байты (баг с офертой
  WB). Генерация картинок: Gemini → Cloudflare FLUX (запаска, ключи
  CLOUDFLARE_ACCOUNT_ID/CLOUDFLARE_API_TOKEN), точная диагностика ошибок (429/403).
  Мёртвая модель gemini-2.0-flash-preview-image убрана. Подсказка про файлы из истории
  теперь срабатывает ТОЛЬКО при явной отсылке (это/картинка/файл/поправь), не на «найди».
- **Запрет фейк-фактов + фильтр поиска (2026-06-05):** агент выдумал номер судебного
  дела «А41-183/2021» (галлюцинация) и пересказал мусорную выдачу (про нотариусов на
  запрос про рекламу). Промпт усилен: ЗАПРЕТ выдумывать номера дел/статьи законов/даты
  без проверки (говорить общими словами + «уточни у юриста»); ФИЛЬТРОВАТЬ результаты
  поиска — не пересказывать нерелевантное, честно сказать «не нашёл». Промпт ~9.2К симв.
  (уже большой — при добавлении новых правил подумать о сокращении).
- **Память о загруженных файлах (2026-06-05):** агент «забывал» присланную картинку
  на follow-up вопросах («а если добавить...»). Фикс в web.py: путь файлов сохраняется
  в истории (поле files в сообщении), и при сообщении БЕЗ файла, если в последних
  ~3 парах был файл-картинка — в контекст агента подставляется путь + указание вызвать
  analyze_image (не просить прислать снова). Работает для любых follow-up, не только
  с ключевыми словами.
- **Анти-угодливость (2026-06-05):** агент льстил и слепо соглашался с сомнительными
  цифрами пользователя («36% — здорово, что следишь!»). Добавлен блок «КРИТИЧЕСКОЕ
  МЫШЛЕНИЕ»: не поддакивать вслепую, при сомнительном факте — отметить расхождение +
  перепроверить через web_search + уточнить источник; не льстить без причины.
- **Цифры с источниками + структура (2026-06-05):** промпт требует привязывать
  каждое число к источнику; при расхождении источников — честно указывать оба;
  числа из памяти помечать «(требует проверки)». Добавлена рекомендуемая структура
  research-ответа (вводная → разделы #### → цифры с источником → расхождения →
  Источники: → вывод → Что дальше?). Эталон — как отвечает Arena-ассистент.
- **Промпт: алгоритм + примеры (2026-06-05):** в SYS_PROMPT добавлен явный
  7-шаговый АЛГОРИТМ анализа запроса (намерение → ясность → нужны ли инструменты →
  план → выполнить → синтез → след.шаг) и блок ПРИМЕРОВ (few-shot: привет, расчёт,
  research, размытый запрос→уточнение, WB→честный отказ). Цель — поведение как у
  Arena-ассистента: тон на «ты», адаптивная длина, честность. Промпт ~6.6К символов. Агент выдумывал, что «посмотрел
  топ-выдачу WB», хотя не вызывал инструменты. Решено УНИВЕРСАЛЬНЫМ правилом в промпте
  (как у Arena-ассистента): запрещено утверждать, что что-то «сделал/посмотрел/нашёл»
  без реального вызова инструмента; если не получилось — честно сказать + предложить
  альтернативы. Костыль-подсказку про конкретно маркетплейсы УБРАЛИ — работает общее
  правило для любых сайтов/действий.
- **read_file Excel/CSV (2026-06-05):** read_file теперь читает .xlsx/.xls/.xlsm
  (openpyxl) и .csv (авто-разделитель/кодировка), помимо текста и PDF.
- **Защита от галлюцинаций (2026-06-03):** промпт запрещает выдумывать цифры
  (комиссии/цены/даты) — только из результатов инструментов; помечать факты из
  общих знаний; добавлять «сверьте с офиц. источником» для тарифов; ставить
  актуальный год 2026. deep_research добавляет памятку в конец материала.
  Единый SYS_PROMPT теперь и в Gemini-ветке (раньше там был куцый промпт).
- **healthcheck.py** с паузами против rate limit и повтором при 429/503.
- Роутинг (`ROUTES`/`FALLBACK` в core.py) перенастроен: Groq в приоритете,
  всё бесплатное.

---

## 5. ⚠️ ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ / ГРАБЛИ

- **429/503 от OpenRouter/Google — это НЕ баг**, а исчерпанный free-лимит.
  Лечится ожиданием (сброс раз в сутки) или переключением на Groq.
- **Groq требует VPN** из РФ. Если у пользователя «Forbidden» — VPN выключен.
- **fetch_url не берёт Авито/Озон/WB/соцсети** — там капча и JS-рендеринг.
  Простой парсер на urllib их не пробьёт. Работает на статьях/блогах/вики.
- **Бесплатные модели OpenRouter периодически меняют ID** → если ломается,
  запусти `healthcheck.py` и сверь живые ID через API OpenRouter.
- **reasoning-модели (gpt-oss)** при малом лимите токенов отдают «пустой ответ» —
  им нужно ≥300 токенов.

---

## 6. 🎯 ПЛАН БУДУЩИХ ДЕЙСТВИЙ (приоритет пользователя)

Пользователь хочет, чтобы агент умел «всё как ассистент Arena», плюс:
собирать под-агентов, парсить Telegram, генерировать изображения/видео,
делать инфографику для маркетплейсов. **Бюджет: только бесплатное.**

Текущий выбор: **сначала довести базу до максимума** (в процессе/готово).

### Очередь задач:
1. **[В РАБОТЕ/ГОТОВО] Прокачка базы** — Groq в function calling, fetch_url,
   усиленный промпт, показ модели. ✅
2. **[СЛЕДУЮЩЕЕ] 🎨 Генерация изображений** — реалистично на бесплатном:
   - Pollinations.ai (без ключа, простой GET-запрос) — самый лёгкий старт
   - HuggingFace Inference (нужен бесплатный токен)
   - Добавить инструмент `generate_image` в agent.py
3. **📬 Парсинг Telegram** — бесплатно через `telethon`:
   - Каналы (публичные) — легко
   - Нужен API_ID/API_HASH с my.telegram.org + номер телефона пользователя
   - Отдельный модуль, т.к. требует авторизации (сессия)
4. **📊 Инфографика для маркетплейсов** — `Pillow`:
   - Берём картинку (своя или сгенерированная) + накладываем текст/плашки
   - «Продающий дизайн» автоматизируется ограниченно — управлять ожиданиями
5. **🤖 Под-агенты** — механизм, где агент создаёт специализированных
   суб-агентов с подмножеством инструментов под конкретную задачу.
6. **🎬 Генерация видео** — ЧЕСТНО: бесплатных API почти нет.
   Veo/Kling/Runway — платные. Низкий приоритет на free-бюджете.

---

## 7. КАК ПРОДОЛЖИТЬ РАБОТУ (для нового агента)

1. Прочитай этот файл + `README.md`.
2. Запроси у пользователя обновлённый `polygraph_update.zip` ИЛИ работай
   с файлами в рабочем пространстве (они актуальны).
3. **Не проси ключи в чат** — пользователь вписывает их в локальный `.env`.
4. Любое изменение моделей → проверяй живость через `healthcheck.py`.
5. После правок: `python -m py_compile *.py` + собери `polygraph_update.zip`
   (9 файлов: core, agent, run, chat, healthcheck, requirements, README,
   .env.example, .gitignore) — пользователь скачивает и распаковывает с заменой.
6. Помни про РФ-контекст и VPN для Groq.
7. Управляй ожиданиями: видео/«продающая инфографика» на free — ограниченно.

---

## ФИКС 2026-06-08: жёсткая красная линия по выдуманным цифрам + разбор скриншотов с данными

**Триггер:** пользователь прислал скриншот кабинета WB Партнёры → Тарифы → Комиссия
(парфюмерия). Реальная комиссия FBS («Маркетплейс, %»): парфюмерная вода / наборы /
селективный парфюм / пробники / блоттеры — **36%**, парфюм для дома (Декор интерьера) —
**33%**, FBW («Склад WB, %») — 32,5% (29,5% для дома), DBS — 25%, EDExpress — 3%, C&C — 22,5%.
Это ДОКАЗАЛО, что ранее выданное агентом (gpt-oss-20b) «17%» — галлюцинация из памяти
модели, да ещё со ссылками на seller.wildberries.ru, который он НЕ открывал.

**Решение пользователя (через ask_user):** правило строгое (вариант "hard") + распространить
на ЛЮБУЮ информацию, а не только тарифы WB.

**Что сделано в `agent.py` (SYS_PROMPT):**
- Добавлена «🔴 КРАСНАЯ ЛИНИЯ ПО ПРОВЕРЯЕМЫМ ДАННЫМ» (универсально): конкретное проверяемое
  значение (%, цена, статистика, курс, дата, имя, характеристика, цитата, номер) называть
  МОЖНО ТОЛЬКО если в ЭТОМ ЖЕ ответе реально вызван инструмент и значение есть в его
  результате. Иначе — НИКАКОГО числа, даже «примерно». Пометка «требует проверки» НЕ
  легализует выдумку для таких данных.
- Явно разрешены общеизвестные стабильные факты (столица, 2+2) — чтобы агент не стал
  параноиком и не отказывался отвечать на банальное.
- Запрет ставить источник (seller.wildberries.ru) рядом с числом, которое не читал.
- Пример-якорь: парфюмерия FBS ~33–36%, НЕ 17%.
- Два новых few-shot примера: отказ назвать комиссию без поиска (ведём в кабинет/предлагаем
  поиск) И корректный ответ ПОСЛЕ реального чтения источника.
- Усилен блок analyze_image: «📊 СКРИНШОТ С ДАННЫМИ — лучший источник истины», переносить
  цифры как есть, не округлять, не путать колонки FBW/FBS/DBS, нечитаемое — честно
  признать. Извлечённые со скрина цифры МОЖНО называть.

**Размер промпта вырос: ~10.4К → 14.2К символов.** ⚠️ Это уже на пределе для слабых
моделей (gpt-oss-20b, qwen). СЛЕДУЮЩИЙ ШАГ при росте — оптимизировать/слить дублирующие
правила, иначе слабая модель будет хуже держать всё одновременно.

### ОПТИМИЗАЦИЯ ПРОМПТА 2026-06-08 (выполнено): 14.2К → 8.87К символов (−37%)
Промпт переписан целиком, без потери смысла. Что слито/убрано:
- 7-шаговый «АЛГОРИТМ РАБОТЫ» сжат в один абзац «🧭 КАК ДУМАТЬ НАД ЗАПРОСОМ» (шаги
  дублировали разделы «УТОЧНЯЙ» и «СТИЛЬ»).
- Раздел «ДОСТОВЕРНОСТЬ» имел 3 перекрывающихся правила про цифры (старое «приводи
  только из инструментов», «КРАСНАЯ ЛИНИЯ», «📎 привязывай к источникам») → оставлена
  одна КРАСНАЯ ЛИНИЯ, остальное влито в неё.
- «ГЛАВНОЕ ПРАВИЛО ЧЕСТНОСТИ ДЕЙСТВИЙ» + «КРИТИЧЕСКОЕ МЫШЛЕНИЕ» сжаты в 2 пункта
  внутри объединённого блока «🛡️ ДОСТОВЕРНОСТЬ И ЧЕСТНОСТЬ».
- Алгоритм поиска (5 шагов) + «фильтруй» + «если данных мало» → блок «🔎 КАЧЕСТВЕННЫЙ
  ПОИСК» из 5 буллетов.
- Few-shot: убраны дублирующие примеры (15% от 3400 влит в правило «сначала ответ»),
  два примера про комиссию WB сохранены (отказ без поиска + корректный ответ после).
Проверено скриптом: ВСЕ 16 ключевых правил на месте (красная линия, 33–36% vs 17%,
запрет фейк-ссылок, скриншот с данными, FBW/FBS/DBS, анти-угодливость, честность
действий, общеизвестные факты разрешены, запрет номеров дел, карточки, русский,
фильтр/приоритет поиска, уточнение, источники). py_compile всех модулей OK, web.py не
трогали. SYS_PROMPT в файле ровно 1, осколков нет.
**ПРОТЕСТИРОВАНО ВЖИВУЮ 2026-06-08 — анти-фейк подтверждён:**
- «комиссия WB FBS» без поиска (llama-3.3) → честный отказ + кабинет ✅
- скриншот тарифов (gpt-oss-120b) → реально извлёк 33–36% ✅
- «столица Франции» → «Париж» сразу (не паранойя) ✅
- «2+2» (gemini) → «4» ✅
- «найди комиссию в интернете» → реально полез в deep_research, дал 3 источника со
  ссылками ✅ НО выдал блоговые 10–20% (устаревшие!) без оговорки про ненадёжность.

### ФИКС «иерархия доверия к источникам» 2026-06-08 (9.78К символов):
Поиск по тарифам WB достаёт только блоги (mpagency, postavleno) — они дают устаревшие
10–20%, а реальные 33–36% в закрытом кабинете поиск прочитать НЕ может. Добавлены 2
правила в блок Достоверность:
1. ИЕРАРХИЯ ДОВЕРИЯ: кабинет/скриншот/офиц.сайт > блоги. Блоговую цифру по тарифам НЕ
   подавать как факт — помечать «по данным блогов ~X%, может быть устаревшим, точное в
   кабинете WB → Тарифы».
2. СВЕРЯЙ С УЖЕ ИЗВЕСТНЫМ В ДИАЛОГЕ: если выше был скриншот кабинета/офиц.источник, а
   поиск даёт другую цифру — не игнорировать конфликт, отметить расхождение «в блогах
   ~10–20%, но по твоему кабинету 33–36% — верь кабинету».
py_compile OK, web.py не трогали. НЕ протестировано вживую после этого фикса — проверить
тем же «найди комиссию в интернете» (должен пометить блоговую цифру как ненадёжную).

**Проверено:** `python3 -m py_compile *.py` OK, web.py не трогали (JS цел).
**НЕ протестировано пользователем вживую** — нужен чистый перезапуск (Остановить.bat → запуск
→ новый чат → Ctrl+Shift+R) и проверка: спросить комиссию WB БЕЗ поиска (должен отказаться
называть число и вести в кабинет), затем прислать скриншот (должен извлечь реальные цифры).

---

## 8. РАБОЧИЙ ПРОЦЕСС ОБМЕНА ФАЙЛАМИ

Пользователь работает в Windows (`C:\Полиграф\polygraph`).
Цикл: агент правит файлы в песочнице → собирает `polygraph_update.zip` →
пользователь скачивает из панели файлов → распаковывает с заменой →
запускает `python chat.py` (с VPN для Groq). `.env` пользователь не трогает.
